#import <stdbool.h>
bool isPalindrome(int x) {
    if(x < 0) return false;
    unsigned int y = 0, z = x;
    do {
        y = (y * 10) + (z % 10);
        z /= 10;
    }
    while(z != 0);
    return y == x;
}
